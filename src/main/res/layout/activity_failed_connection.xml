<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_failed_connection"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.nickjm6.picontroller.failedConnection">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="32sp"
        android:id="@+id/sorry"
        android:text="Sorry..."/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/sorry"
        android:textSize="22sp"
        android:layout_marginTop="150dp"
        android:id="@+id/message"
        android:text="Connection to Server has failed, Check that you are on the same connection as the Raspberry Pi, and that the Raspberry Pi is on."/>

    <Button
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:layout_below="@+id/message"
        android:layout_marginTop="150dp"
        android:textSize="32sp"
        android:text="Try Again"
        android:id="@+id/tryagain"
        android:onClick="tryAgain"
        android:background="#0044FF"/>

</RelativeLayout>
